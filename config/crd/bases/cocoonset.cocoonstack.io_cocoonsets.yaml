---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: cocoonsets.cocoonset.cocoonstack.io
spec:
  group: cocoonset.cocoonstack.io
  names:
    categories:
    - cocoon
    kind: CocoonSet
    listKind: CocoonSetList
    plural: cocoonsets
    shortNames:
    - cs
    - cocoonsets
    singular: cocoonset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.readyAgents
      name: Ready
      type: integer
    - jsonPath: .status.desiredAgents
      name: Desired
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: CocoonSet is the Schema for the cocoonsets API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CocoonSetSpec defines the desired state of a CocoonSet.
            properties:
              agent:
                description: AgentSpec defines the configuration for agent VMs in
                  a CocoonSet.
                properties:
                  backend:
                    default: cloud-hypervisor
                    description: |-
                      Backend selects the hypervisor backend used to run a VM.
                      Firecracker uses direct kernel boot and only supports OCI VM images
                      (cloudimg URLs and Windows are rejected); the webhook and vk-cocoon
                      enforce these constraints at admission and run time.
                    enum:
                    - cloud-hypervisor
                    - firecracker
                    type: string
                  connType:
                    description: |-
                      ConnType is the connection protocol advertised for a VM. Empty
                      falls back to OS-based inference (Linux→ssh, Windows→rdp, Android→adb);
                      set explicitly to override (e.g. Linux+xrdp→rdp).
                    enum:
                    - ssh
                    - rdp
                    - vnc
                    - adb
                    type: string
                  envFrom:
                    items:
                      description: EnvFromSource represents the source of a set of
                        ConfigMaps or Secrets
                      properties:
                        configMapRef:
                          description: The ConfigMap to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                        prefix:
                          description: |-
                            Optional text to prepend to the name of each environment variable.
                            May consist of any printable ASCII characters except '='.
                          type: string
                        secretRef:
                          description: The Secret to select from
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret must be defined
                              type: boolean
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                    type: array
                  forcePull:
                    description: ForcePull bypasses the image cache and re-pulls from
                      upstream.
                    type: boolean
                  image:
                    minLength: 1
                    type: string
                  mode:
                    default: clone
                    description: AgentMode defines the mode of an agent VM.
                    enum:
                    - clone
                    - run
                    type: string
                  network:
                    description: Network selects the cluster network to attach the
                      VM to.
                    type: string
                  os:
                    default: linux
                    description: OSType defines the guest operating system type.
                    enum:
                    - linux
                    - windows
                    - android
                    type: string
                  replicas:
                    default: 0
                    description: Replicas is the number of sub-agents; the main agent
                      is always created in addition.
                    format: int32
                    minimum: 0
                    type: integer
                  resources:
                    description: ResourceRequirements describes the compute resource
                      requirements.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  serviceAccountName:
                    type: string
                  storage:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Storage sizes the VM root volume.
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                required:
                - image
                type: object
              nodePool:
                default: default
                type: string
              snapshotPolicy:
                default: always
                description: SnapshotPolicy defines when VM snapshots are taken.
                enum:
                - always
                - main-only
                - never
                type: string
              suspend:
                type: boolean
              toolboxes:
                items:
                  description: ToolboxSpec defines the configuration for a toolbox
                    VM in a CocoonSet.
                  properties:
                    backend:
                      default: cloud-hypervisor
                      description: |-
                        Backend selects the hypervisor backend used to run a VM.
                        Firecracker uses direct kernel boot and only supports OCI VM images
                        (cloudimg URLs and Windows are rejected); the webhook and vk-cocoon
                        enforce these constraints at admission and run time.
                      enum:
                      - cloud-hypervisor
                      - firecracker
                      type: string
                    connType:
                      description: |-
                        ConnType is the connection protocol advertised for a VM. Empty
                        falls back to OS-based inference (Linux→ssh, Windows→rdp, Android→adb);
                        set explicitly to override (e.g. Linux+xrdp→rdp).
                      enum:
                      - ssh
                      - rdp
                      - vnc
                      - adb
                      type: string
                    forcePull:
                      description: ForcePull bypasses the image cache and re-pulls
                        from upstream.
                      type: boolean
                    image:
                      type: string
                    mode:
                      default: run
                      description: ToolboxMode defines the mode of a toolbox VM.
                      enum:
                      - run
                      - clone
                      - static
                      type: string
                    name:
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    network:
                      description: Network selects the cluster network to attach the
                        VM to.
                      type: string
                    os:
                      default: linux
                      description: OSType defines the guest operating system type.
                      enum:
                      - linux
                      - windows
                      - android
                      type: string
                    resources:
                      description: ResourceRequirements describes the compute resource
                        requirements.
                      properties:
                        claims:
                          description: |-
                            Claims lists the names of resources, defined in spec.resourceClaims,
                            that are used by this container.

                            This field depends on the
                            DynamicResourceAllocation feature gate.

                            This field is immutable. It can only be set for containers.
                          items:
                            description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                            properties:
                              name:
                                description: |-
                                  Name must match the name of one entry in pod.spec.resourceClaims of
                                  the Pod where this field is used. It makes that resource available
                                  inside a container.
                                type: string
                              request:
                                description: |-
                                  Request is the name chosen for a request in the referenced claim.
                                  If empty, everything from the claim is made available, otherwise
                                  only the result of this request.
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                          x-kubernetes-list-map-keys:
                          - name
                          x-kubernetes-list-type: map
                        limits:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Limits describes the maximum amount of compute resources allowed.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                        requests:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: |-
                            Requests describes the minimum amount of compute resources required.
                            If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                            otherwise to an implementation-defined value. Requests cannot exceed Limits.
                            More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                          type: object
                      type: object
                    staticIP:
                      type: string
                    staticVMID:
                      type: string
                    storage:
                      anyOf:
                      - type: integer
                      - type: string
                      description: Storage sizes the VM root volume.
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    vncPort:
                      format: int32
                      maximum: 65535
                      minimum: 0
                      type: integer
                  required:
                  - name
                  type: object
                type: array
            required:
            - agent
            type: object
          status:
            description: CocoonSetStatus represents the observed state of a CocoonSet.
            properties:
              agents:
                items:
                  description: AgentStatus represents the observed state of a single
                    agent VM.
                  properties:
                    forkedFrom:
                      type: string
                    ip:
                      type: string
                    phase:
                      type: string
                    podName:
                      type: string
                    role:
                      type: string
                    slot:
                      format: int32
                      type: integer
                    vmID:
                      type: string
                    vmName:
                      type: string
                  required:
                  - role
                  - slot
                  type: object
                type: array
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              desiredAgents:
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: CocoonSetPhase represents the lifecycle phase of a CocoonSet.
                enum:
                - Pending
                - Running
                - Scaling
                - Suspended
                - Failed
                type: string
              readyAgents:
                format: int32
                type: integer
              toolboxes:
                items:
                  description: ToolboxStatus represents the observed state of a single
                    toolbox VM.
                  properties:
                    connType:
                      description: |-
                        ConnType is the connection protocol advertised for a VM. Empty
                        falls back to OS-based inference (Linux→ssh, Windows→rdp, Android→adb);
                        set explicitly to override (e.g. Linux+xrdp→rdp).
                      enum:
                      - ssh
                      - rdp
                      - vnc
                      - adb
                      type: string
                    ip:
                      type: string
                    name:
                      type: string
                    phase:
                      type: string
                    podName:
                      type: string
                    vmID:
                      type: string
                    vmName:
                      type: string
                    vncPort:
                      format: int32
                      type: integer
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
