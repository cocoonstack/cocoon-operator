name: build

on:
  push:
    branches:
      - main
    paths-ignore:
      - 'deploy/**'
      - '*.md'
      - 'LICENSE'

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: 'go.mod'

      - name: Build
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: '0'
        run: |
          VERSION=$(git describe --tags $(git rev-list --tags --max-count=1) 2>/dev/null || echo dev)
          REVISION=$(git rev-parse HEAD)
          BUILTAT=$(date +%Y-%m-%dT%H:%M:%S)
          LDFLAGS="-s -X github.com/cocoonstack/cocoon-operator/version.VERSION=${VERSION} \
                   -X github.com/cocoonstack/cocoon-operator/version.REVISION=${REVISION} \
                   -X github.com/cocoonstack/cocoon-operator/version.BUILTAT=${BUILTAT}"
          go build -ldflags "${LDFLAGS}" -o cocoon-operator-${{ matrix.goos }}-${{ matrix.goarch }} .

      - name: Upload binary artifact
        uses: actions/upload-artifact@v6
        with:
          name: cocoon-operator-${{ matrix.goos }}-${{ matrix.goarch }}
          path: cocoon-operator-${{ matrix.goos }}-${{ matrix.goarch }}
          retention-days: 30
